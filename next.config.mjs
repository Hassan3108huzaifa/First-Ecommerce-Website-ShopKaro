
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'shopkaro-hassanrj.vercel.app'],
        unoptimized: true,

      },
};

export default nextConfig;
